# Al descargar el archivo si el nombre es diferente renombrar a "Automatizacion Excel"
# Crea un entorno en la python terminal de python
python -m venv env
# Descargar librerias en el entorno (pip install pandas sqlalchemy openpyxl)
pandas → manejo de datos
SQLAlchemy → bases de datos
openpyxl → Excel
# Ejecutar en la terminal python src/main.py --Ejecutar en la terminal del entorno
# Una vez ejecutable el archivo en Power BI abrir Editor de Power Query
# Cambiar origen al excel generado en el output
# Finalmente Aplicatr y cerrar cambios y Actualizar el dasboard de power BI
